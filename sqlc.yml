version: "2"
sql:
  - engine: "postgresql"
    queries: "schema/query"
    schema: "schema/migrations"
    gen:
      go:
        package: "query"
        out: "internal/database/query"
        sql_package: "pgx/v5"
